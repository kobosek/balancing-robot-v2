#pragma once

// BalancingAlgorithm includes ESP-IDF's check header for the firmware build,
// but the production source does not use a check macro.  Keep the host
// adapter deliberately empty instead of reimplementing ESP-IDF behavior.
